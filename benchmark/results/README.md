# Benchmark results

`latest.json` is the deterministic synthetic-dataset correctness and PyTorch
comparison. `native_latest.json` is the latest native kernel speed gate from
the development machine. Numbers are evidence for the current iteration, not
portable performance promises; rerun both harnesses on every target.

Memory plans generated for the two benchmark graphs are under `memory_plans/`.
